{
  "name": "moewalls-cli",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "MOEWALLS_RUNTIME_MODE=development bun run --watch src/index.ts",
    "start": "MOEWALLS_RUNTIME_MODE=development bun run src/index.ts",
    "build": "bun run scripts/patch-sixel-upng.mjs && bun build src/index.ts --outdir dist --target bun --minify --external playwright --external playwright-core",
    "build:binary": "bun run scripts/patch-sixel-upng.mjs && bun build src/index.ts --compile --outfile dist/moewalls --external playwright --external playwright-core",
    "install:browsers": "./scripts/install-browsers.sh"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ink": "^6.3.1",
    "ink-picture": "^2.0.1",
    "ink-text-input": "^6.0.0",
    "playwright": "^1.52.0",
    "react": "^19.2.0",
    "react-devtools-core": "^7.0.1"
  }
}
